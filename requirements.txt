# Core FastAPI
fastapi==0.115.0
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0

# Supabase client (includes PostgreSQL drivers)
supabase==2.0.3
psycopg2-binary==2.9.9

# Geolocation
geopy==2.4.0

# Email
# Resend is called over its plain HTTP API via httpx (below) rather than the
# `resend` SDK: the SDK is sync-only and would put blocking I/O back into the
# request path. jinja2 renders the HTML templates.
jinja2==3.1.2

# HTTP client (also the Resend transport)
httpx==0.24.1

# Environment variables
python-dotenv==1.0.0

# CORS & Multipart
python-multipart==0.0.6

# Payment Gateway
razorpay==1.4.1
setuptools==70.0.0  # Updated for Python 3.13 compatibility

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
email-validator==2.1.0
bcrypt==4.1.1
slowapi==0.1.9  # Rate limiting

# Utilities
python-dateutil==2.8.2
pytz==2023.3

rich==13.7.0  # Rich terminal output for logging

# HTML sanitisation (blog article bodies)
# Rust-backed ammonia binding; the maintained successor to the deprecated
# `bleach`. Blog HTML is server-rendered into the public page, so the body is
# cleaned against an allowlist on every write.
nh3==0.2.20

# Cloudinary Storage
cloudinary==1.41.0

# Testing (CI + local integration tests)
pytest==8.3.4
pytest-asyncio==0.24.0
